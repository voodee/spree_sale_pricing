<% if item_added.variant.product.msrp.present? && discount_percentage(item_added.variant) != '0%' %>
  <span>
    <strong>
      <span class="line-through">
      <%= number_to_currency(product.master.msrp) %>
      </span>
      &nbsp;
    </strong>
  </span>
  <span class="sale-price">
    <span class="price selling" itemprop="price">
      <strong><%= display_price(item_added.variant) %></strong>
    </span>
  </span>
  <span class="save-percent">
    Save <%= discount_percentage(item_added.variant) %>
  </span>
<% else %>
  <span class="list-price">
    <strong><%= display_price(item_added.variant) %></strong>
  </span>
<% end %>
